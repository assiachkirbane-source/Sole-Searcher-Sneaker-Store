{
  "name": "sole-searcher-store",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp metadata.json dist/metadata.json && esbuild index.tsx --bundle --outfile=dist/bundle.js --format=esm --platform=browser --define:process.env.NODE_ENV='\"production\"'"
  },
  "dependencies": {
    "@angular/common": "^18.0.0",
    "@angular/compiler": "^18.0.0",
    "@angular/core": "^18.0.0",
    "@angular/forms": "^18.0.0",
    "@angular/platform-browser": "^18.0.0",
    "@angular/router": "^18.0.0",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "typescript": "^5.9.3"
  }
}